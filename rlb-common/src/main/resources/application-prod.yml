server:
  port: 8083

spring:
  application:
    name: rlb-common

eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka-service:8761/eureka
  instance:
    instance-id: ${spring.application.name}:${random.value}